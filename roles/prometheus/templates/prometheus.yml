global:
  scrape_interval:
  evaluation_interval:


scrape_configs:
  - job_name: "ec2-nodes"
    ec2_sd_configs:
      - region: us-east-1
        port: 9100
        filters:
          - name: tag:monitor
            values:  ["yes", "true"]

